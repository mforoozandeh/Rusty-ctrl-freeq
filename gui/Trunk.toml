[build]
target = "index.html"
dist = "dist"
# Relative asset paths, so the application works at any base URL: the root of a domain, a subdirectory, or a laptop
# running `python -m http.server`.
public_url = "./"

[serve]
port = 8080
open = false
